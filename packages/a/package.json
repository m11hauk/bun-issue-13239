{
  "name": "package-a",
  "version": "0.0.1",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "echo 'package a build started running' && sleep 5 && echo 'package a build finished running'"
  },
  "dependencies": {
    "bun-types": "latest",
    "zod": "1"
  }
}
